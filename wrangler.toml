name = "soly-birthday"
compatibility_date = "2026-06-11"
type = "javascript"

# للملفات الثابتة (Static Files)
[assets]
directory = "./dist/public"

# للـ Build Command (اختياري)
build = { command = "pnpm build", cwd = "." }

# متغيرات البيئة (اختياري)
[env.production]
[env.production.vars]
ENVIRONMENT = "production"